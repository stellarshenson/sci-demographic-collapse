# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - Project initialization** (v0.1.0): Initialized Claude Code configuration for the `sci-demographic-collapse` data science project via `/init-project`<br>
   **Result**: Project was scaffolded from `copier-data-science` v1.3.9 (Python 3.13, `uv`, `ruff`, `pytest`, Jupyter kernel support; module `sci_demographic_collapse` under `src/`) with git already initialized but no commits and no `.claude/` directory. Created `.claude/CLAUDE.md` importing workspace config from `/home/lab/workspace/.claude/CLAUDE.md`, with the mandatory reinforced bans (no auto tags/version-bumps/publishing/commits, Makefile-first installs) and append-only journal rules stated verbatim. Added project context (demographic-collapse domain terminology - TFR, replacement level, age structure, population momentum), the technology stack, and strengthened rules emphasizing notebook-resident experiments, `notebook-standards` and `datascience:hypothesis` skills, and `data/raw` immutability. Left the existing customized README.md and `.gitignore` untouched; did not commit (awaiting explicit approval). Scaffolded this journal via `/journal:create`.

2. **Task [Extended] - Notebook 1: demographic-collapse model on GPU** (v0.1.0): Ingested a 109-turn shared design conversation and built the first executed solution notebook<br>
   **Result**: Ingested a claude.ai share link (headless `google-chrome` + Playwright rendering the client-side SPA past Cloudflare, then ordered user/assistant turn extraction) carrying the 109-turn design dialogue that is this project's intellectual foundation. Built `notebooks/01-kj-demographic-collapse.ipynb` implementing the nine-state coupled ODE system (n, μ_b, σ²_b, ρ, C, τ, E, Ψ, D) with couple-bound births, an Allee floor, an absorbing extinction pivot at n=0, and a quadratic (threshold-triggered) childlessness contagion - the bistable "lock" earlier drafts lacked. Per the user's `Use GPU` directive the numeric core was ported to `torch` on the RTX PRO 4000 Blackwell (pinned by UUID, `CUDA_DEVICE_ORDER=PCI_BUS_ID`): a batched RK4 marches 10^4 trajectories in parallel (14,400-IC basin sweep + 4,000-set parameter sweep), and `torch.func.jacrev` gives exact autodiff Jacobians for det(J)/eigenvalues; the agent-based model stays CPU `numpy` (dynamic births). Toolchain: swapped pandas→polars, added `scikit-learn` (GP surrogate) and `torch` to `pyproject.toml`, installed via `make install` (disk hit 100%, torch corrupted; fixed with `uv pip install --reinstall` after the user restored space). Structural, uncalibrated findings: the Seldon manifold is PRESENT (genuine bistability, sharp separatrix - 99% clean fate-flip on the logarithmic reprobe); the pivot is a fold (dominant social eigenvalue rides to zero at the steepest fall, extinction is an absorbing/degenerate slow-manifold state); top drivers are the childlessness contagion and the coupling brake; the ABM shows selection lifting the fertility trait while the childless norm spreads and the population collapses, with critical-slowing-down early warnings. Calibration and full bibliography deferred to Notebook 2; no real-world numerical claims drawn (model uncalibrated). Notebook executes end-to-end, exit 0, GPU-accelerated.
